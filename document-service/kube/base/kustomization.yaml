# KUBE-3 — document-service base. No Secret needed (no DB, no external
# credentials in local-backend mode).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - pvc.yaml
  - service.yaml
