<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.perf.metricsconsumer</groupId>
    <artifactId>jmeter-metrics-consumer</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>jmeter-metrics-consumer</name>
    <description>
        Ingests WorkerMetricBatch JSON envelopes POSTed by the
        local-orchestrator workers (POST /api/v1/ingest) and lands
        per-second rows plus the rollup deltas in the Oracle metrics
        schema the orchestrators read.
    </description>

    <properties>
        <!-- Boot's parent compiles with the release flag set to ${java.version} (17 by default),
             which silently overrides source/target. This is what makes it 21. -->
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- POST /api/v1/ingest is the sole ingest path, and its body is JSON
             decoded by Spring Boot's own Jackson — this service carries no
             serialization dependency at all. -->
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <!-- ORACLE-MIGRATION (2026-08-28): the metrics schema lives in Oracle.
             ojdbc11 is BOM-managed (oracle-database.version); the same build
             is baked into the jmeter-cloud-flyway image so the Job and the
             service share one driver. -->
        <dependency>
            <groupId>com.oracle.database.jdbc</groupId>
            <artifactId>ojdbc11</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- SLIMDOWN (2026-07-20) — no metrics/tracing stack in this service:
             the hosting infra provides observability out of the box.
             Deliberately absent: micrometer-registry-prometheus,
             micrometer-tracing-bridge-otel, opentelemetry-exporter-otlp,
             spring-boot-starter-aop (sole consumer was the @Observed aspect).
             Logs (below) + /actuator/{health,info} are the whole built-in
             surface — do not reintroduce a registry or tracer. -->

        <!-- OBSERVABILITY Phase A — structured JSON logging.
             KEPT by SLIMDOWN — logs are the debug/RCA surface.
             logstash-logback-encoder version pinned explicitly:
             spring-boot-dependencies 3.5.x does NOT manage this artifact
             (only janino is BOM-managed). 7.4 matches the pin in the
             jmeter-local-orchestrator + jmeter-global-orchestrator +
             document-service poms so all services emit identical JSON
             record shapes. The encoder layers onto the existing
             AsyncAppender + bounded-queue stack in logback-spring.xml
             (the OOM-safety layer from incident 2026-05-15 is preserved).
             janino powers the <if> conditional that flips between JSON
             (default) and Spring Boot's pretty pattern when DEV_MODE_LOG=true. -->
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>8.1</version>
        </dependency>
        <dependency>
            <groupId>org.codehaus.janino</groupId>
            <artifactId>janino</artifactId>
        </dependency>
        <!-- springdoc-openapi — serves Swagger UI at /swagger-ui.html and the
             OpenAPI JSON at /v3/api-docs (auto-generated from @RestController
             annotations). The hand-curated YAML lives at api/openapi.yaml and
             is bundled into the JAR via the <resource> mapping in <build>;
             springdoc.swagger-ui.url=/openapi.yaml in application.yml points
             the UI at the curated spec. Per the cross-component contract,
             every REST-bearing service ships this. -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.9.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- ORACLE-MIGRATION OM-6 — the opt-in database contract tests
             (`mvn test -PdbTests`, JUnit tag `db`): Testcontainers boots
             Oracle Free, Flyway applies the real migrations from
             ../oracle/migrations, and the tests exercise the SQL the default
             loop never touches. All test scope; nothing here ships. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>oracle-free</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-oracle</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}-${project.version}</finalName>
        <!-- Bundle api/openapi.yaml into the JAR at static/openapi.yaml so
             Spring Boot's static-resource handler serves it at /openapi.yaml
             (which Swagger UI is configured to load via application.yml). -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
            <resource>
                <directory>${project.basedir}/api</directory>
                <targetPath>static</targetPath>
                <includes>
                    <include>openapi.yaml</include>
                </includes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <!-- The `db`-tagged contract tests need Docker and ~1 min for
                 Oracle Free to boot, so the default loop skips them; the
                 dbTests profile below runs ONLY them. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <excludedGroups>db</excludedGroups>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- ORACLE-MIGRATION OM-6 — `mvn test -PdbTests` runs the `db`-tagged
             contract tests against a Testcontainers Oracle Free and nothing
             else. Requires Docker. -->
        <profile>
            <id>dbTests</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <excludedGroups combine.self="override"/>
                            <groups>db</groups>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!-- SECURITY S-14 — OWASP dependency CVE gate. Inert by default so the
             offline `mvn -o ...` flow is unaffected; activate online in CI:
               mvn -P securityScan verify
             First run downloads the NVD database (slow); speed it up with a key:
               mvn -P securityScan -DnvdApiKey=$NVD_API_KEY verify
             (request one at https://nvd.nist.gov/developers/request-an-api-key).
             Fails the build on any dependency with CVSS >= 7 (high/critical).
             NOTE: added offline — the scan itself has NOT been run in this
             environment; verify (and bump the plugin version to latest) on the
             first CI run. -->
        <profile>
            <id>securityScan</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <version>12.2.2</version>
                        <configuration>
                            <failBuildOnCVSS>7</failBuildOnCVSS>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
